"""fleetlab — explainable capacity, autoscaling, cost, and placement simulation.

Files in -> models -> files out. No daemon, no server, no network at runtime.
Simulation != production: every published number carries a stated uncertainty
and a provenance flag (measured / source-reported / assumed).
"""

__version__ = "0.1.0"
